package grekiki1;

import static org.junit.Assert.*;
import org.junit.Test;

import battlecode.common.*;

public class RobotPlayerTest{

	@Test public void testSanity(){
		/*
		assertEquals(2,1+1);
		vector_gl2 v=new vector_gl2();
		for(int i=0;i<10;i++) {
			for(int j=0;j<10;j++) {
				v.add(new MapLocation(i,j));
			}
		}
		assertEquals(v.get(1),new MapLocation(0,1));
		int size=100;
		for(int i=0;i<10;i++) {
			for(int j=0;j<20;j++) {
				v.remove(new MapLocation(9-i,j%10));
				if(i<10&&j<10) {
					size--;
				}
				assertEquals(v.size,size);
			}
		}
		*/
	}

}
